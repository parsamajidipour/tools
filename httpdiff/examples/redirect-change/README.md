# Example: redirect-change

Demonstrates HTTPDIFF-REDIRECT-001: the redirect target changes from a
relative, same-origin path to an absolute, cross-origin, HTTP (non-TLS)
destination.

Run:

    httpdiff compare baseline.txt candidate.txt
