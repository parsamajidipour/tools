Fixture files used by unit/integration tests can be placed here. Most
current tests construct raw HTTP responses inline for clarity; this
directory is reserved for larger fixture files as the suite grows.
